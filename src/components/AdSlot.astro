---
export interface Props {
  id: string
  className?: string
}
const { id, className = '' } = Astro.props
---

<div id={`ad-slot-${id}`} class={`ad-slot ${className}`} style="display: none;"></div>
